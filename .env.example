# Copy this file to ".env" and fill in the values
# Afterwards you can start the dev server normally (see README steps).

# PostgreSQL database connection string (Supabase, local Postgres, etc.)
DATABASE_URL="postgres://user:password@host:5432/database"

# OpenAI integration key (or AI_INTEGRATIONS_OPENAI_API_KEY)
AI_INTEGRATIONS_OPENAI_API_KEY="sk-xxxx"
# Optionally provide a custom base URL for OpenAI (e.g. for proxy or GovCloud)
AI_INTEGRATIONS_OPENAI_BASE_URL="https://api.openai.com/v1"

# You can also set a generic key just in case
# OPENAI_API_KEY="sk-xxxx"

# Port the server should listen on (defaults to 5000)
# PORT=5000
